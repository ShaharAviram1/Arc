import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Search() {
  return (
    <PagePlaceholder
      title="Search / add"
      description="Search AniList and add a show to your list in a chosen status."
    />
  )
}
