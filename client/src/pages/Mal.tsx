import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Mal() {
  return (
    <PagePlaceholder
      title="MAL link / sync log"
      description="Connect MyAnimeList, view the write log, and revert a write."
    />
  )
}
