import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Invite() {
  return (
    <PagePlaceholder
      title="Accept invite"
      description="Redeem an invite token and set up your Arc account."
    />
  )
}
