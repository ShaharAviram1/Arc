import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Admin() {
  return (
    <PagePlaceholder
      title="Admin"
      description="Users and invites, rules, jobs, disk, and the review queue."
    />
  )
}
