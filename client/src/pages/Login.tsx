import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Login() {
  return (
    <PagePlaceholder
      title="Login"
      description="Email + password sign-in, and the invite-token flow for new accounts."
    />
  )
}
