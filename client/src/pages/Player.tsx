import { PagePlaceholder } from '@/components/PagePlaceholder'

export function Player() {
  return (
    <PagePlaceholder
      title="Player"
      description="HLS player with resume, next episode, and progress reporting."
    />
  )
}
