"""SQLAlchemy models, one module per aggregate.

M1 adds every table in architecture.md §4: users, invites, sessions, anime,
episodes, media_files, renditions, list_entries, watch_progress, mal_links,
mal_write_log, wants, torrents, jobs, rec_runs, settings.
"""
